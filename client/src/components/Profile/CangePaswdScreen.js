import React from 'react'
import { View, Text } from 'react-native'

const PaswdScreen = ({ navigation }) => {
  return (
    <View style={{flex:1,justifyContent:'center',alignItems:'center'}}>
      <Text>Paswd Screen</Text>
    </View>
  )
}

export default PaswdScreen;